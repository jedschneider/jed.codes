version: 1
frontend:
  phases:
    build:
      commands:
        - hugo --minify
        - npx -y pagefind --site public
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
